language: python
python:
  - 2.7
install:
  - pip install -q -r requirements/test.txt --use-mirrors
  - pip install coveralls --use-mirrors
script:
  - coverage run teagarden/manage.py test --settings=settings.test teagarden
after_success:
  - coveralls
notifications:
  email:
    - henning.kage@gmail.com
